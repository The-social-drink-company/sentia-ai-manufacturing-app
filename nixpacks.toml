[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build:railway"
]

[start]
cmd = "node server.js"

[variables]
NODE_ENV = "production"
RAILWAY_DEPLOYMENT = "true"
