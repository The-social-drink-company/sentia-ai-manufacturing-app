[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm install --production=false']

[phases.build] 
cmds = ['npm run build']

[phases.start]
cmd = 'node start-production.js'

[variables]
NODE_ENV = 'production'
PORT = '$PORT'
MCP_PORT = '9001'