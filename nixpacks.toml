[phases.setup]
nixPkgs = ['nodejs_20']

[providers]
node = true

[phases.install]
cmds = ['npm ci --production=false']

[phases.build] 
cmds = [
  'npm run build'
]

[phases.start]
cmd = 'node server.js'

[variables]
NODE_ENV = 'production'
PORT = '$PORT'
MCP_PORT = '9001'
RAILWAY_ENVIRONMENT = 'production'