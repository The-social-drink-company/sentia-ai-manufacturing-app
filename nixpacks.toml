[phases.setup]
nixPkgs = ["nodejs_20", "npm-10_x"]

[phases.install]
cmds = ["rm -rf node_modules/.cache", "npm ci --verbose"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "node server.js"