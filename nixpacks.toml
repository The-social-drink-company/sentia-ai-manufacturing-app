[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build:railway"
]

[start]
cmd = "npm run start"

[variables]
NODE_ENV = "production"
