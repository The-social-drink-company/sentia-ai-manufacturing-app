/**
 * Auth Middleware - Stub for Integration Tests
 *
 * This is a compatibility stub that redirects to the actual auth.js middleware.
 * Created to resolve import path issues in server/routes/auth.js
 */

import { authMiddleware } from './auth.js'

export default authMiddleware
