export function listApprovals(req, res) {
  res.status(501).json({ message: 'Admin approvals endpoint not implemented yet.' })
}

export function submitApproval(req, res) {
  res.status(501).json({ message: 'Admin approval submission endpoint not implemented yet.' })
}
