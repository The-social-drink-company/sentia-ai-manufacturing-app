# Minimal Nixpacks config for debugging Railway 502
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install express"]

[start]
cmd = "node railway-health-test.js"