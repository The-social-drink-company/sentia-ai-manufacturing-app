# PRODUCTION NIXPACKS CONFIG - OPTIMIZED FOR RAILWAY

[phases.setup]
nixPkgs = ["nodejs_18"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "npm run build",
  "echo 'Build completed successfully'",
  "ls -la dist/"
]

[start]
cmd = "node index.cjs"