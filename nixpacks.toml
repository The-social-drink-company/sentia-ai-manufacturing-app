[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['cd mcp-server && npm ci --production=false']

[phases.build] 
cmds = ['echo "No build step required for MCP server"']

[phases.start]
cmd = 'cd mcp-server && npm start'

[variables]
NODE_ENV = 'production'
PORT = '$PORT'
RAILWAY_ENVIRONMENT = 'production'