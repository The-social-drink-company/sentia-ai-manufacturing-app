[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['npm ci --production=false']

[phases.build] 
cmds = ['FORCE_COLOR=0 npm run build']

[phases.start]
cmd = 'node server.js'

[variables]
NODE_ENV = 'test'
PORT = '$PORT'
RAILWAY_ENVIRONMENT = 'testing'