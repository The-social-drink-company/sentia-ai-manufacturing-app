[providers]
node = "18"

[phases.setup]
nixPkgs = ['nodejs_18']

[phases.install]
cmds = ['npm ci']

[phases.build] 
cmds = ['npm run build']

[phases.start]
cmd = 'node server.js'

[staticAssets]
from = []

[variables]
PORT = '$PORT'