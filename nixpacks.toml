# Railway Nixpacks Configuration
[phases.setup]
nixPkgs = ['nodejs_23']

[phases.install]
cmds = ['npm ci --omit=dev']

[phases.build]
cmds = ['npm run build']

[phases.start]
cmd = 'node server.js'

[variables]
NODE_ENV = 'test'
PORT = '$PORT'
RAILWAY_ENVIRONMENT = 'testing'