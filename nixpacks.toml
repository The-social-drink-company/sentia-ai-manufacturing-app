# Railway Nixpacks Configuration - Simplified
# Just build and run Node.js, no Caddy

[build]
cmds = ["npm ci", "npm run build"]

[start]
cmd = "node railway-ultimate.js"

[variables]
NIXPACKS_NODE_VERSION = "22.12.0"