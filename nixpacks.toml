# nixpacks.toml
# Configuration for Railway deployment using Nixpacks  
# Node.js 18 ONLY - Fix version collision

# Only use Node.js provider - exclude Python
providers = ["node"]

[phases.setup]
# Only Node.js 18 packages - no Python - no Node 22 collision
nixPkgs = ["nodejs-18_x", "npm-9_x", "openssl"]

[phases.install]
cmds = ["npm ci --prefer-offline --no-audit"]

[phases.build]
# Skip cache mount entirely to avoid conflicts
cmds = ["npm run build"]
cacheDirectories = []

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"
# Cache bust to force rebuilds with correct Node version
BUILD_CACHE_VERSION = "v2-node18-fix"