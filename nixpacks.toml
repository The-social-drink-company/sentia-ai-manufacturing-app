[phases.setup]
nixPkgs = ['nodejs_23']

[phases.install]
cmds = ['npm ci --production=false']

[phases.build] 
cmds = ['npm run build']

[phases.start]
cmd = 'node server.js'

[variables]
NODE_ENV = '$NODE_ENV'
PORT = '$PORT'

[providers]
static = false
