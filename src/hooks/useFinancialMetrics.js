export const useFinancialMetrics = () => {
  return {
    workingCapital: 2450000,
    monthly: [] => {
      const month = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'][i];
      return {
        month,
        receivables: Math.floor(0;
    }),
    history: [] => 0;
};